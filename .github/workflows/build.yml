name: build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    name: build
    steps:
      - name: "build-in-docker"
        uses: ../actions/docker_action@v1
        with:
          run-command: ".ci/build.sh"
